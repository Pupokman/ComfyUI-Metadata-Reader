(async()=>{const b64=window.__CMR_APP_B64||'';delete window.__CMR_APP_B64;const bin=atob(b64);const bytes=Uint8Array.from(bin,c=>c.charCodeAt(0));const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const source=await new Response(stream).text();(0,eval)(source);})().catch(error=>{console.error('Failed to load application:',error);document.body.innerHTML='<pre style="color:#fff;background:#111;padding:20px">Failed to load application: '+String(error)+'</pre>';});
